import 'package:flutter/material.dart';
class SendOutDropDownButton extends StatelessWidget {
  const SendOutDropDownButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [
          Text('Hi',style: TextStyle(fontWeight: FontWeight.bold,fontSize: 16),),

        ],
      ),
    );
  }
}
